export function updateFavicon(faviconUrl: string): void {
  // TODO: Dynamically update the browser favicon to the given URL
  // Hint: find or create a <link rel="icon"> element in document.head
  void faviconUrl; // suppresses unused-parameter error -- remove when implementing
}

export function updateDocumentTitle(productName: string): void {
  // TODO: Update the document's <title> tag to reflect the current tenant
  void productName; // suppresses unused-parameter error -- remove when implementing
}
